// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v7.35.1
// source: payment/types/stripe.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "payment.stripe";

export interface StripePaymentSession {
  sessionId: string;
  publicKey: string;
  /** Stripe hosted checkout URL (optional) */
  url: string;
  /** Amount in the smallest unit (e.g., cents) */
  amount: number;
  /** I */
  currency: string;
}

export interface StripeResolveRequest {
  sessionId: string;
}

export interface StripeResolveResponse {
  paymentIntentId: string;
  verified: boolean;
}

export const PAYMENT_STRIPE_PACKAGE_NAME = "payment.stripe";

function createBaseStripePaymentSession(): StripePaymentSession {
  return { sessionId: "", publicKey: "", url: "", amount: 0, currency: "" };
}

export const StripePaymentSession: MessageFns<StripePaymentSession> = {
  encode(message: StripePaymentSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.publicKey !== "") {
      writer.uint32(18).string(message.publicKey);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    if (message.amount !== 0) {
      writer.uint32(168).int64(message.amount);
    }
    if (message.currency !== "") {
      writer.uint32(178).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StripePaymentSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStripePaymentSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.amount = longToNumber(reader.int64());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseStripeResolveRequest(): StripeResolveRequest {
  return { sessionId: "" };
}

export const StripeResolveRequest: MessageFns<StripeResolveRequest> = {
  encode(message: StripeResolveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StripeResolveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStripeResolveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseStripeResolveResponse(): StripeResolveResponse {
  return { paymentIntentId: "", verified: false };
}

export const StripeResolveResponse: MessageFns<StripeResolveResponse> = {
  encode(message: StripeResolveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paymentIntentId !== "") {
      writer.uint32(10).string(message.paymentIntentId);
    }
    if (message.verified !== false) {
      writer.uint32(16).bool(message.verified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StripeResolveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStripeResolveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paymentIntentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.verified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
