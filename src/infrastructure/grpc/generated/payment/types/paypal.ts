// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v7.35.1
// source: payment/types/paypal.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "payment.paypal";

export interface PayPalPaymentSession {
  orderId: string;
  /** PayPal redirect URL */
  approvalLink: string;
  /** Amount in the smallest unit (e.g., cents) */
  amount: number;
  /** ISO 4217 currency code (e.g., "USD") */
  currency: string;
}

export interface PayPalResolveRequest {
  orderId: string;
}

export interface PayPalResolveResponse {
  verified: boolean;
}

export const PAYMENT_PAYPAL_PACKAGE_NAME = "payment.paypal";

function createBasePayPalPaymentSession(): PayPalPaymentSession {
  return { orderId: "", approvalLink: "", amount: 0, currency: "" };
}

export const PayPalPaymentSession: MessageFns<PayPalPaymentSession> = {
  encode(message: PayPalPaymentSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId !== "") {
      writer.uint32(10).string(message.orderId);
    }
    if (message.approvalLink !== "") {
      writer.uint32(18).string(message.approvalLink);
    }
    if (message.amount !== 0) {
      writer.uint32(168).int64(message.amount);
    }
    if (message.currency !== "") {
      writer.uint32(178).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PayPalPaymentSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePayPalPaymentSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.approvalLink = reader.string();
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.amount = longToNumber(reader.int64());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBasePayPalResolveRequest(): PayPalResolveRequest {
  return { orderId: "" };
}

export const PayPalResolveRequest: MessageFns<PayPalResolveRequest> = {
  encode(message: PayPalResolveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId !== "") {
      writer.uint32(10).string(message.orderId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PayPalResolveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePayPalResolveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBasePayPalResolveResponse(): PayPalResolveResponse {
  return { verified: false };
}

export const PayPalResolveResponse: MessageFns<PayPalResolveResponse> = {
  encode(message: PayPalResolveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.verified !== false) {
      writer.uint32(8).bool(message.verified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PayPalResolveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePayPalResolveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.verified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
